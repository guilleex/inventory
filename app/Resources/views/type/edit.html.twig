{% extends 'default/setings.html.twig' %}

{% block body %}
	<section class="col-xs-6 col-xs-offset-3 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-1 col-lg-4 col-lg-offset-2" id="machine-setings-form">
			<h3>Edit Type</h3>
			{{ include('type/_form.html.twig') }}
			{{ form_start(delete_form) }}
			   <button type="submit" class="btn btn-danger btn-xs del pull-right" onclick="return confirm('Are you sure?');">Delete</button>
			{{ form_end(delete_form) }}
	</section>
{% endblock %}