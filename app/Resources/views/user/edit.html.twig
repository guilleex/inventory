{% extends 'base.html.twig' %}

{% block title %}
	{{ user.fullName }}
{% endblock %}

{% block body %}
<div class="col-xs-8 col-xs-offset-2 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3" id="user-edit">
	<h2>{{ user.fullName }}</h2>

	{{ include('user/userForm.html.twig') }}

	{{ form_start(delete_form) }}
	<button type="submit" class="btn btn-danger btn-xs del" onclick="return confirm('Are you sure?');">Delete</button>
	{{ form_end(delete_form) }}
</div>
{% endblock %}